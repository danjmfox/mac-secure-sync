---
id: DR--20251113--config--config-governance
dateCreated: '2025-11-13'
version: 1.1.0
status: superseded
changeType: creation
domain: config
slug: config-governance
changelog:
  - date: '2025-11-13'
    note: Initial creation
  - date: '2026-09-01'
    note: Marked superseded by ADR-001 (config schema v2) — decision text below is historical record, not current behavior
---
# DR--20251113--config--config-governance
⚙️ DR: Configuration governance — single config file under ~/.config/securelocal

**Status: Superseded by [ADR-001](../../../docs/product/architecture/adr-001-config-schema-v2.md).** The `multi-usb-sync` feature (2026-05-17) replaced the single-file, single-device `config.env` model below with a YAML schema (`~/.config/securelocal/config.yaml`, schema v2) supporting multiple directories and multiple USB devices. The centralization *principle* this DR established — one canonical config file, generated by the installer, no environment-variable drift — still holds; only the file format and shape changed. Left below as historical record.

Context
Environment variables and configuration drift cause errors in automated scripts.

Decision
Centralise configuration in ~/.config/securelocal/config.env, generated by the installer.
Scripts source this file at runtime and validate all required variables.

Consequences
	•	One canonical source of truth.
	•	Simple to back up, inspect, or recreate.
	•	Prevents silent misconfiguration.
	•	Changes require reinstall or manual edit (by design).

Notes
Future enhancements might include per-environment overrides or schema validation.
