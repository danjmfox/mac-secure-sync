#!/usr/bin/env bash
# __SCAFFOLD__ = true
# sync-cloud.sh — RED scaffold
# Reads config.yaml, rclone-syncs each directory to its configured cloud remote.
# Triggered by launchd StartInterval or invoked manually.
#
# This stub exits 1 with a NOT YET IMPLEMENTED message so acceptance tests
# fail RED (not BROKEN). Replace this file with the real implementation.

echo "NOT YET IMPLEMENTED -- RED scaffold" >&2
exit 1
